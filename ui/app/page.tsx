'use client'

/**
 * Single Next page: Hermes mount_spa falls back to this index.html for all
 * deep links; TanStack Router inside Providers owns the real paths.
 */
export default function HomePage() {
  return null
}
