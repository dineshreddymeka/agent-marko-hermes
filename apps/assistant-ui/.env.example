# assistant-ui agent endpoint (same-origin rewrite → Hermes :9119/agui)
# Leave unset to use /agui via next.config.ts rewrites.
# NEXT_PUBLIC_AGUI_AGENT_URL=/agui

# Hermes backend origin used by Next rewrites (server-side)
HERMES_URL=http://127.0.0.1:9119
